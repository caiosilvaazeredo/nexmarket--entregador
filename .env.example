# Nexmarket Entregador — environment

# Google Maps API key (Android needs it for the in-app map).
# After setting this, also paste it into app.json:
#   android.config.googleMaps.apiKey  and  ios.config.googleMapsApiKey
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY="YOUR_GOOGLE_MAPS_API_KEY"

# The Firebase project is shared with the loja app and lives in
# firebase-config.json (committed on purpose, mirroring the loja repo).
# No Firebase secrets are needed here for client usage.

# ------------------------- Pagamentos (Stripe Connect) -----------------------
# URL do servidor de pagamentos da plataforma (repo nexmarket--Empresa, pasta
# server/). Com ela preenchida a Carteira mostra o cadastro de recebimento via
# Stripe Connect (onboarding) e os repasses podem cair direto na conta do
# entregador. Vazia → apenas o fluxo manual de saques (PIX pela Empresa).
EXPO_PUBLIC_PAYMENTS_API_URL=""

# ------------------------- Login com Google ---------------------------------
# IDs OAuth criados em https://console.cloud.google.com/apis/credentials
# (espelhe também em app.json → expo.extra.googleAuth). Sem eles o botão
# degrada com uma mensagem amigável.
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=""
EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID=""
EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID=""
